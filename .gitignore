# ------------------------------------
# 1. 의존성 폴더 (설치 파일) 무시
# ------------------------------------
# Node.js 프로젝트의 모든 라이브러리가 설치되는 폴더
node_modules/

# 클라이언트와 서버 폴더 내부에 있는 node_modules도 명시적으로 무시 (안전성 확보)
client/node_modules/
server/node_modules/


# ------------------------------------
# 2. 보안 파일 (절대 외부에 공개 금지) 무시
# ------------------------------------
# 환경 변수 파일 (DB 주소, 비밀 키 포함)
.env
client/.env
server/.env


# ------------------------------------
# 3. 빌드 및 출력 폴더 무시
# ------------------------------------
# React 빌드 결과물
client/build/
# 서버에서 발생할 수 있는 로그 파일
*.log
npm-debug.log*


# ------------------------------------
# 4. OS 및 IDE 관련 파일 무시
# ------------------------------------
# macOS 시스템 파일
.DS_Store
# Visual Studio Code 설정 폴더
.vscode/